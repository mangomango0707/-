<!-- 背景图 -->
<view class="player-container" style="background: url({{picUrl}}) no-repeat center/cover"></view>
<view class="player-mask"></view>

<view class="player-info">
  <!-- 唱片图片 -->
  <view class="player-disc {{isPlaying ? 'play' : ''}}" bind:tap="onChangeLyricShow" hidden="{{isLyricShow}}">
    <image class="player-img rotation {{isPlaying ? '' : 'rotation-paused'}}" src="{{picUrl}}"></image>
  </view>

  <!-- 引入歌词组件 -->
  <my-lyric class="lyric" isLyricShow="{{!isLyricShow}}" bind:tap="onChangeLyricShow" lyric="{{lyric}}" />

  <!-- 引入进度条组件 -->
  <view class="progress-bar">
    <my-progress-bar bind:musicEnd="onNext" bind:timeUpdate="timeUpdate" bind:musicPlay="onPlay" bind:musicPause="onPause" isSame="{{isSame}}" />
  </view>

  <!-- 播放器控制面板 -->
  <view class="control">
    <text class="iconfont icon-shangyishou" bind:tap="onPrev"></text>
    <text class="iconfont {{isPlaying ? 'icon-zanting' : 'icon-shipin'}}" bind:tap="togglePlaying"></text>
    <text class="iconfont icon-xiayishou" bind:tap="onNext"></text>
  </view>
</view>